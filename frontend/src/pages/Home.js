import React from 'react';
import ProductList from '../components/ProductList';

const Home = () => {
    return (
        <main className="text-center">
            <h1>Home</h1>
            <ProductList />
        </main>
    );
};

export default Home;
